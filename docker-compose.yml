services:
  web:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
    volumes:
      - todo-data:/app/data
    restart: unless-stopped

volumes:
  todo-data:
